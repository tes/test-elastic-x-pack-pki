# Elasticsearch X-Pack PKI auth test

This repo is using Terraform to generate SSL assets (certs and keys) and a usable `docker-compose.yml`.

## Running the services

```
$ terraform init
$ terraform apply
$ docker-compose up
```
